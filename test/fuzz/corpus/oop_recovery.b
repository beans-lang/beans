abstract class MissingBrace {
    abstract fn value( -> int

singleton class {
    priv static = 1
    priv static fn hidden( {
    inout fn broken<T(value: T {
        self.value +=
    }
}

struct Cell<T {
    priv value T
    priv inout fn store(value: T {
    inout fn replace(value: T
}

class Child extends implements {
    override abstract fn value() ->
}
