// The module root of a library, named after its module rather than `main.b`
// or `lib.b`, and importing none of its own packages. Both are legal and both
// are common — crema is shaped exactly like this — and together they are what
// used to leave every other file in the project with no snapshot at all.

package semlib
