// Two selections binding the same name stop the load, like two module
// imports binding the same name always have.
import {parse} from std.encoding.json
import {parse} from std.encoding.xml

fn main() { }
